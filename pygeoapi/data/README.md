Place local raster files for pygeoapi coverage collections here.

For the `myr` collection, add your GeoTIFF as:
- `pygeoapi/data/myr.tif`

This directory is mounted into the pygeoapi container at `/pygeoapi/data` by `dev/docker-compose.yml`.
