## Code style
This repo uses Biome for linting/formatting. Before finishing any change:
- Run `npm run lint-fix` to auto-fix and format.
- Do not disable rules with inline comments unless explicitly asked.
- Config lives in `biome.json` — follow it, don't reinvent style choices.
